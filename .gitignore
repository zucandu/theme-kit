node_modules/
dist/
.playwright-mcp/
*.log

# Screenshots taken while verifying the kit — not part of the package.
/home*.png
/product*.png
/cart*.png
/checkout*.png
/account*.png

# A theme dropped inside the kit for convenience. It is the developer's own
# project — usually with its own git history — and does not belong in this repo.
/theme/
